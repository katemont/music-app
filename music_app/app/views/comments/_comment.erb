<br><br><br><em><%= comment.body %></em><br>
- <i><%= comment.user.name %> &nbsp;&middot;&nbsp; <%= time_ago_in_words(comment.created_at) %> ago</i><br><br>

<% if can? :update, comment %>
  <%= link_to "Edit", edit_track_comment_path(@track, comment) %>
<% end %>
<% if can? :destroy, comment %>
  |<%= link_to 'Delete this comment', [@track, comment], method: :delete %> 
<% end %>

| <%= pluralize comment.flags.count, "Flag" %>
<% if current_user && can?(:create, Flag) %>
  | <%= link_to "Flag", track_comment_flag_path(@track, comment), method: "post" %> <br><br><br><br>

<% end %>